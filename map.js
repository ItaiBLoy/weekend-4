function map(array){
    let newArray = [];
    for (let i = 0; i < array.length; i++){
        newArray.push(array[i])//Any condition or manupulation can be done during this loop
    }
    return newArray;
}