function basicMath(operator, x, y){
    switch(operator) {
        case '+':
            return x+y
        case '-':
            return x-y
        case '/':
            return x/y
        case '*':
            return x*y
      }
}